#include "tensorstore/driver/registry.h"  // confirm this is the right header

// Use the macro name you found in registry.h, and the *actual* driver id:
TENSORSTORE_USE_DRIVER(netcdf);  // <-- replace macro or string as needed
