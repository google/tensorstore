# Defines additional platforms needed for the build.

platform(
    name = "windows_x86_64_mingw",
    constraint_values = [
        "@bazel_tools//tools/cpp:mingw",
        "@platforms//os:windows",
        "@platforms//cpu:x86_64",
    ],
)
